{
  "schema": "ao.atlas.verification-evidence.v0.1",
  "node_id": "mission-recommendation-month2-contract-convergence-32",
  "task_id": "mission-recommendation-month2-contract-convergence-32-task",
  "status": "passed",
  "public_safety_scan_passed": true,
  "commands": [
    "ulimit -n 4096; cargo test -p ao2-cp-server --test gc_smoke --offline",
    "ulimit -n 4096; cargo test --workspace --offline",
    "ulimit -n 4096; cargo clippy --workspace --all-targets --offline -- -D warnings",
    "go test ./... -count=1 in ao-atlas",
    "go vet ./... in ao-atlas",
    "go build -o target/atlas-node32-verification ./cmd/atlas",
    "scripts/atlas-foundry-roundtrip-smoke.sh",
    "scripts/production-readiness.sh",
    "git diff --check",
    "mission recommendations validate-evidence",
    "mission recommendations run-link-digest-check",
    "scoped public-safety scan over the complete wave root"
  ],
  "required_commands": [
    "go test ./... -count=1",
    "go vet ./...",
    "go build ./cmd/atlas",
    "scripts/atlas-foundry-roundtrip-smoke.sh",
    "scripts/production-readiness.sh",
    "mission recommendations validate-evidence",
    "mission recommendations run-link-digest-check",
    "git diff --check",
    "scoped public-safety scan over the complete wave root"
  ],
  "repair_notes": [
    "Default soft file-descriptor limit 256 caused burst integration failures; a shell-local ulimit -n 4096 rerun passed the full workspace and did not change system or repository configuration.",
    "GC evidence proves atomic individual publication and bounded prune behavior, not crash-transactional recovery across file deletion and index rewrite.",
    "Atlas build output will be written below target and removed before the working-tree check.",
    "Atlas full verification, control-plane source audit, and scoped Sentinel scan passed with unsafe_match_count=0."
  ],
  "rsi_remains_denied": true,
  "objective": "Coordinate ao2-control-plane Control plane transactional GC test through a bounded Atlas compatibility evidence node with no promotion."
}
