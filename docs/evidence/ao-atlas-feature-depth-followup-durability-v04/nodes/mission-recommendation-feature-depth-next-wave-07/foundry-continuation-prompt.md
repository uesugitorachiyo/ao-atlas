# Foundry continuation for mission-recommendation-feature-depth-next-wave-07

Execute exactly one bounded node: Validate required node evidence fields before recommendation readback advances.

RSI remains denied. No provider calls, credential inspection, release, deploy, publish, upload, tag, dependency update, policy widening, or direct main mutation.
