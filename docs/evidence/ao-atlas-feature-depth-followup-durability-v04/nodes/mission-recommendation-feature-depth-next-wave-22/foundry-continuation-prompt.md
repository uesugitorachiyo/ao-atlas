# Foundry continuation for mission-recommendation-feature-depth-next-wave-22

Execute exactly one bounded node: Add scoped public safety scans for changed evidence and prompt artifacts.

RSI remains denied. No provider calls, credential inspection, release, deploy, publish, upload, tag, dependency update, policy widening, or direct main mutation.
