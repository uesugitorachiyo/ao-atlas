# Foundry continuation for mission-recommendation-feature-depth-next-wave-17

Execute exactly one bounded node: Generate compaction resume prompts that preserve lease timing and active node state.

RSI remains denied. No provider calls, credential inspection, release, deploy, publish, upload, tag, dependency update, policy widening, or direct main mutation.
