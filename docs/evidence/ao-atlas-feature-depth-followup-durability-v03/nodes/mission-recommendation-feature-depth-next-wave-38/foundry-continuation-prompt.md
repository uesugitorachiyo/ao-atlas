# Foundry continuation for mission-recommendation-feature-depth-next-wave-38

Execute exactly one bounded node: Add next wave prompt generation with minimum two hour work budget language.

RSI remains denied. No provider calls, credential inspection, release, deploy, publish, upload, tag, dependency update, policy widening, or direct main mutation.
