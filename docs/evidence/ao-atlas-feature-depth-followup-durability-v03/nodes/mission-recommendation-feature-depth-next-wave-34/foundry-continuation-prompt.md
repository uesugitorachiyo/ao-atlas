# Foundry continuation for mission-recommendation-feature-depth-next-wave-34

Execute exactly one bounded node: Add dashboard provenance links for Foundry Promoter Command and Sentinel evidence.

RSI remains denied. No provider calls, credential inspection, release, deploy, publish, upload, tag, dependency update, policy widening, or direct main mutation.
