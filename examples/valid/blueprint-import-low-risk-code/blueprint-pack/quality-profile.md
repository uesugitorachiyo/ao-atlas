# Quality Profile

Use test-first changes, public-safe fixtures, digest-bound evidence, and
read-only gate outputs. The import must fail closed when Blueprint authorization
is missing, blocked, stale, digest-mismatched, or out of scope.

Private paths must be represented only by `excluded/` placeholders.
